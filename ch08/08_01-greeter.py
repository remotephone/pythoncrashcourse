# Define the function. Information in the () is passed to the function
def greet_user(username):
    # Docstring - used for documentation of functions
    """Display a simple greeting."""
    print("Hello, " + username.title() + "!")

# Call the function
greet_user('jesse')
